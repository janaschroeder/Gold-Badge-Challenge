using Challenge_1_Repo;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;
using System.Collections.Generic;

namespace Challenge1_Unit_TestsUnitTestProject1
{
    [TestClass]
    public class UnitTest1
    {
    }
}
        /*[TestMethod]
        public void Seed()
        {
            _repo = new _Meal_Repo();
            List<Meal> _menu = new List<Meal>();

            Meal kiddieMeal = new Meal(
    1,
    "Kiddie Meal",
    "Just right for youngsters and smaller appetites",
    "Small Cheeseburger, Small Fries, Small Coke",
    2.95
                    );
            Meal adultCombo = new Meal(
                2,
                "Adult Combo",
                "Classic American fare",
                "Cheeseburger, Fries, Coke",
                3.95
                )
                _menu.AddMealToMenu(kiddieMeal);
            _menu.AddMealToMenu(adultCombo);
        }
    }

    */
    /*[TestMethod]

    static void AddTest()
    {

        // Arrange
                //StreamingContent content = new StreamingContent(
        List<Meal> _menu = new List<Meal>();

        "Galaxy Quest",
            "Sci Fi actors inadvertently go on a real Sci Fi adventure",
            Maturity.PG13,
            5,
            GenreType.Comedy
                );

        // Act
        bool wasAdded = _repo.AddContentToDirectory(content);

        Console.WriteLine(_repo.Count);

        Console.WriteLine(wasAdded);
        Console.WriteLine(content.Title);

        // Assert
        Assert.IsTrue(wasAdded);
    }
    GetMenu()


    [TestMethod]
    static void FindByTitle_ShouldGetCorrectContent()
    {
        // Arrange
        // Act
        StreamingContent searchResult = _repo.GetContentByTitle("the room");

        // Assert
        Assert.AreEqual(searchResult, theRoom);
    }



    [TestMethod]
    static void DeleteContent_ShouldDelete()
    {
        bool wasRemoved = _repo.DeleteContent("the room");
        Assert.IsTrue(wasRemoved);

        bool wasAlsoRemoved = _repo.DeleteContent("fkjhgjhkgkjh");
        Assert.IsFalse(wasAlsoRemoved);
    }
}
*/
